export interface Area {
  i: number;  // index
  n: string;  // name
  s: string;  // state
  z: number;  // zone
  l: string;  // link
  ac: number; // acres
  ve: number; // vertical
  an: number; // annual snowfall
  li: number; // lifts
  ru: number; // runs
  pk: number; // peak elevation
  ba: number; // base elevation
}
